{% if not object.hasBeenReplied %}
    <a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('reply', object) }}" title="{{ 'Reply' | trans }}">
        <i class="far fa-envelope"></i>
    </a>
{% endif %}

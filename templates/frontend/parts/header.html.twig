{% block header %}
    <header class="w-full py-6 px-8 border-b bg-white">
        <div class="flex justify-between items-center max-w-screen-xl mx-auto">
            <h1 class="text-2xl font-bold">{{ project_title }}</h1>
            <div class="hidden lg:flex items-center space-x-6">
                <a href="{{ path(enum('App\\Enum\\RoutesEnum').app_project_illustrations_index.name) }}" class="hover:underline">{{ 'Illustrations' | trans }}</a>
                <a href="{{ path(enum('App\\Enum\\RoutesEnum').app_project_workshops_index.name) }}" class="hover:underline">{{ 'Workshops' | trans }}</a>
                <a href="#" class="hover:underline">{{ 'About' | trans }}</a>
                <a href="#" class="hover:underline">{{ 'Contact' | trans }}</a>
                <a href="https://www.instagram.com/miguel.bustos/" target="_blank" class="text-2xl w-6">
                    {{ ux_icon('bi:instagram') }}
                </a>
                <div class="hidden lg:flex items-center space-x-1 text-sm font-semibold">
                    <a href="#" class="hover:underline">EN</a>
                    <span>·</span>
                    <a href="#" class="hover:underline">CAT</a>
                    <span>·</span>
                    <a href="#" class="hover:underline text-gray-400">ES</a>
                </div>
            </div>
            <div class="lg:hidden flex items-center">
                <a href="https://www.instagram.com/miguel.bustos/" target="_blank" class="text-2xl w-6 me-6">
                    {{ ux_icon('bi:instagram') }}
                </a>
                <a href="#" class="text-3xl w-16 p-3">
                    {{ ux_icon('bi:list') }}
                </a>
            </div>
        </div>
    </header>
    {% include'@App/frontend/parts/mobile_menu.html.twig' %}
{% endblock %}
